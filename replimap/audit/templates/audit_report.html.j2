<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>RepliMap Security Audit Report</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        /* Custom styles for code blocks */
        .code-block {
            font-family: 'Menlo', 'Monaco', 'Courier New', monospace;
            font-size: 0.8rem;
            line-height: 1.5;
        }
        .code-block .affected {
            background-color: rgba(239, 68, 68, 0.1);
        }
        /* Collapsible sections */
        details > summary {
            cursor: pointer;
            list-style: none;
        }
        details > summary::-webkit-details-marker {
            display: none;
        }
        details > summary::before {
            content: '▶';
            display: inline-block;
            margin-right: 0.5rem;
            transition: transform 0.2s;
        }
        details[open] > summary::before {
            transform: rotate(90deg);
        }
        /* Print styles */
        @media print {
            .no-print { display: none; }
            .page-break { page-break-before: always; }
        }
    </style>
</head>
<body class="bg-gray-50 text-gray-900">
    <!-- Header -->
    <header class="bg-gradient-to-r from-indigo-600 to-purple-600 text-white py-8">
        <div class="container mx-auto px-6">
            <div class="flex items-center justify-between">
                <div>
                    <h1 class="text-3xl font-bold">🔒 Security Audit Report</h1>
                    <p class="mt-2 text-indigo-100">Generated by RepliMap</p>
                </div>
                <div class="text-right text-sm">
                    <p><strong>Account:</strong> {{ account_id }}</p>
                    <p><strong>Region:</strong> {{ region }}</p>
                    {% if profile %}<p><strong>Profile:</strong> {{ profile }}</p>{% endif %}
                    {% if vpc_id %}<p><strong>VPC:</strong> {{ vpc_id }}</p>{% endif %}
                    <p class="mt-2 text-indigo-200">{{ generated_at }}</p>
                </div>
            </div>
        </div>
    </header>

    <main class="container mx-auto px-6 py-8">
        <!-- Executive Summary -->
        <section class="mb-10">
            <h2 class="text-2xl font-bold mb-6 text-gray-800">📊 Executive Summary</h2>

            <div class="grid grid-cols-1 md:grid-cols-4 gap-6">
                <!-- Score Card -->
                <div class="bg-white rounded-xl shadow-lg p-6 border-l-4
                    {% if score >= 80 %}border-green-500{% elif score >= 60 %}border-yellow-500{% else %}border-red-500{% endif %}">
                    <div class="text-center">
                        <div class="text-5xl font-bold
                            {% if score >= 80 %}text-green-600{% elif score >= 60 %}text-yellow-600{% else %}text-red-600{% endif %}">
                            {{ score }}%
                        </div>
                        <div class="mt-2 text-lg font-medium text-gray-600">Security Score</div>
                        <div class="mt-1">
                            <span class="inline-block px-3 py-1 rounded-full text-sm font-bold
                                {% if grade == 'A' %}bg-green-100 text-green-800
                                {% elif grade == 'B' %}bg-blue-100 text-blue-800
                                {% elif grade == 'C' %}bg-yellow-100 text-yellow-800
                                {% elif grade == 'D' %}bg-orange-100 text-orange-800
                                {% else %}bg-red-100 text-red-800{% endif %}">
                                Grade: {{ grade }}
                            </span>
                        </div>
                    </div>
                </div>

                <!-- Passed -->
                <div class="bg-white rounded-xl shadow-lg p-6">
                    <div class="flex items-center">
                        <div class="p-3 bg-green-100 rounded-full">
                            <svg class="w-8 h-8 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"></path>
                            </svg>
                        </div>
                        <div class="ml-4">
                            <div class="text-3xl font-bold text-green-600">{{ passed }}</div>
                            <div class="text-gray-600">Checks Passed</div>
                        </div>
                    </div>
                </div>

                <!-- Failed -->
                <div class="bg-white rounded-xl shadow-lg p-6">
                    <div class="flex items-center">
                        <div class="p-3 bg-red-100 rounded-full">
                            <svg class="w-8 h-8 text-red-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12"></path>
                            </svg>
                        </div>
                        <div class="ml-4">
                            <div class="text-3xl font-bold text-red-600">{{ failed }}</div>
                            <div class="text-gray-600">Issues Found</div>
                        </div>
                    </div>
                </div>

                <!-- Severity Breakdown -->
                <div class="bg-white rounded-xl shadow-lg p-6">
                    <div class="text-sm font-medium text-gray-600 mb-3">Severity Breakdown</div>
                    <div class="space-y-2">
                        {% if critical_count > 0 %}
                        <div class="flex justify-between items-center">
                            <span class="px-2 py-0.5 bg-purple-100 text-purple-800 text-xs font-medium rounded">CRITICAL</span>
                            <span class="font-bold text-purple-800">{{ critical_count }}</span>
                        </div>
                        {% endif %}
                        {% if high_count > 0 %}
                        <div class="flex justify-between items-center">
                            <span class="px-2 py-0.5 bg-red-100 text-red-800 text-xs font-medium rounded">HIGH</span>
                            <span class="font-bold text-red-800">{{ high_count }}</span>
                        </div>
                        {% endif %}
                        {% if medium_count > 0 %}
                        <div class="flex justify-between items-center">
                            <span class="px-2 py-0.5 bg-yellow-100 text-yellow-800 text-xs font-medium rounded">MEDIUM</span>
                            <span class="font-bold text-yellow-800">{{ medium_count }}</span>
                        </div>
                        {% endif %}
                        {% if low_count > 0 %}
                        <div class="flex justify-between items-center">
                            <span class="px-2 py-0.5 bg-blue-100 text-blue-800 text-xs font-medium rounded">LOW</span>
                            <span class="font-bold text-blue-800">{{ low_count }}</span>
                        </div>
                        {% endif %}
                        {% if critical_count == 0 and high_count == 0 and medium_count == 0 and low_count == 0 %}
                        <div class="text-sm text-green-600 font-medium">
                            ✓ No issues found
                        </div>
                        {% endif %}
                    </div>
                </div>
            </div>
        </section>

        {% if high_severity %}
        <!-- High Severity Findings Alert -->
        <section class="mb-10">
            <div class="bg-red-50 border-l-4 border-red-500 rounded-lg p-6">
                <div class="flex items-center">
                    <svg class="w-8 h-8 text-red-600 mr-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"></path>
                    </svg>
                    <div>
                        <h3 class="text-lg font-bold text-red-800">⚠️ {{ high_severity|length }} High/Critical Severity Issues Require Immediate Attention</h3>
                        <p class="text-red-700 mt-1">These issues pose significant security risks and should be addressed as a priority.</p>
                    </div>
                </div>
                <div class="mt-4 space-y-2">
                    {% for finding in high_severity[:5] %}
                    <div class="flex items-center text-sm">
                        <span class="px-2 py-0.5 rounded text-xs font-medium {{ finding.severity | severity_badge }} mr-2">{{ finding.severity }}</span>
                        <span class="font-mono text-red-700">{{ finding.check_id }}</span>
                        <span class="mx-2 text-gray-400">-</span>
                        <span class="text-gray-700">{{ finding.check_name }}</span>
                    </div>
                    {% endfor %}
                    {% if high_severity|length > 5 %}
                    <p class="text-sm text-red-600 mt-2">... and {{ high_severity|length - 5 }} more high severity issues below</p>
                    {% endif %}
                </div>
            </div>
        </section>
        {% endif %}

        <!-- SOC2 Control Mapping -->
        {% if soc2_summary %}
        <section class="mb-10">
            <h2 class="text-2xl font-bold mb-6 text-gray-800">📋 SOC2 Control Mapping</h2>
            <div class="bg-white rounded-xl shadow-lg overflow-hidden">
                <table class="min-w-full divide-y divide-gray-200">
                    <thead class="bg-gray-50">
                        <tr>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Control</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Category</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Issues</th>
                            <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Status</th>
                        </tr>
                    </thead>
                    <tbody class="bg-white divide-y divide-gray-200">
                        {% for control, info in soc2_summary.items() %}
                        <tr>
                            <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900">{{ control }}</td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">{{ info.category }}</td>
                            <td class="px-6 py-4 whitespace-nowrap">
                                <span class="px-2 py-1 text-xs font-medium bg-red-100 text-red-800 rounded-full">{{ info.count }} issue{% if info.count != 1 %}s{% endif %}</span>
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap">
                                <span class="px-2 py-1 text-xs font-medium bg-yellow-100 text-yellow-800 rounded-full">Needs Review</span>
                            </td>
                        </tr>
                        {% endfor %}
                    </tbody>
                </table>
            </div>
        </section>
        {% endif %}

        <!-- All Findings -->
        <section class="mb-10 page-break">
            <h2 class="text-2xl font-bold mb-6 text-gray-800">🔍 All Findings ({{ all_findings|length }})</h2>

            <div class="space-y-4">
                {% for enriched in all_findings %}
                {% set finding = enriched.finding %}
                <div class="bg-white rounded-xl shadow-lg overflow-hidden border-l-4
                    {% if finding.severity == 'CRITICAL' %}border-purple-500
                    {% elif finding.severity == 'HIGH' %}border-red-500
                    {% elif finding.severity == 'MEDIUM' %}border-yellow-500
                    {% else %}border-blue-500{% endif %}">

                    <details class="group">
                        <summary class="px-6 py-4 flex items-center justify-between hover:bg-gray-50">
                            <div class="flex items-center space-x-4">
                                <span class="px-2 py-1 text-xs font-bold rounded border {{ finding.severity | severity_badge }}">
                                    {{ finding.severity }}
                                </span>
                                <div>
                                    <span class="font-mono text-sm font-bold text-gray-700">{{ finding.check_id }}</span>
                                    <span class="mx-2 text-gray-400">|</span>
                                    <span class="text-gray-900">{{ finding.check_name }}</span>
                                </div>
                            </div>
                            <div class="text-sm text-gray-500">
                                {{ finding.resource }}
                            </div>
                        </summary>

                        <div class="px-6 py-4 border-t border-gray-100 bg-gray-50">
                            <div class="grid grid-cols-1 lg:grid-cols-2 gap-6">
                                <!-- Details -->
                                <div>
                                    <h4 class="font-medium text-gray-900 mb-3">Details</h4>
                                    <dl class="space-y-2 text-sm">
                                        <div class="flex">
                                            <dt class="w-24 text-gray-500">Resource:</dt>
                                            <dd class="font-mono text-gray-700">{{ finding.resource }}</dd>
                                        </div>
                                        <div class="flex">
                                            <dt class="w-24 text-gray-500">File:</dt>
                                            <dd class="font-mono text-gray-700">{{ finding.file_path }}</dd>
                                        </div>
                                        <div class="flex">
                                            <dt class="w-24 text-gray-500">Lines:</dt>
                                            <dd class="font-mono text-gray-700">{{ finding.file_line_range[0] }}-{{ finding.file_line_range[1] }}</dd>
                                        </div>
                                        {% if enriched.soc2_control %}
                                        <div class="flex">
                                            <dt class="w-24 text-gray-500">SOC2:</dt>
                                            <dd class="text-gray-700">{{ enriched.soc2_control }} ({{ enriched.soc2_category }})</dd>
                                        </div>
                                        {% endif %}
                                        {% if finding.guideline %}
                                        <div class="flex">
                                            <dt class="w-24 text-gray-500">Guide:</dt>
                                            <dd><a href="{{ finding.guideline }}" target="_blank" class="text-indigo-600 hover:text-indigo-800 underline">Documentation ↗</a></dd>
                                        </div>
                                        {% endif %}
                                    </dl>
                                </div>

                                <!-- Code Snippet -->
                                {% if enriched.code_snippet %}
                                <div>
                                    <h4 class="font-medium text-gray-900 mb-3">Affected Code</h4>
                                    <pre class="code-block bg-gray-900 text-gray-100 p-4 rounded-lg overflow-x-auto text-xs">{{ enriched.code_snippet }}</pre>
                                </div>
                                {% endif %}
                            </div>

                            <!-- Fix Suggestion -->
                            {% if enriched.fix_suggestion %}
                            <div class="mt-6">
                                <h4 class="font-medium text-gray-900 mb-3">💡 Suggested Fix</h4>
                                <div class="relative">
                                    <pre class="code-block bg-green-900 text-green-100 p-4 rounded-lg overflow-x-auto text-xs">{{ enriched.fix_suggestion }}</pre>
                                    <button onclick="copyToClipboard(this)" class="no-print absolute top-2 right-2 px-3 py-1 bg-green-700 hover:bg-green-600 text-white text-xs rounded transition">
                                        Copy
                                    </button>
                                </div>
                            </div>
                            {% endif %}
                        </div>
                    </details>
                </div>
                {% endfor %}
            </div>
        </section>

        <!-- CTA Section -->
        <section class="mb-10 no-print">
            <div class="bg-gradient-to-r from-indigo-500 to-purple-600 rounded-xl shadow-lg p-8 text-white text-center">
                <h2 class="text-2xl font-bold mb-4">Need Help Fixing These Issues?</h2>
                <p class="text-indigo-100 mb-6 max-w-2xl mx-auto">
                    Our team can help you remediate security findings, implement best practices, and achieve compliance.
                </p>
                <a href="mailto:support@replimap.dev?subject=Security%20Audit%20Remediation%20Help"
                   class="inline-block px-8 py-3 bg-white text-indigo-600 font-bold rounded-lg hover:bg-indigo-50 transition">
                    Contact Us for Remediation Support
                </a>
            </div>
        </section>
    </main>

    <!-- Footer -->
    <footer class="bg-gray-800 text-gray-300 py-8">
        <div class="container mx-auto px-6 text-center text-sm">
            <p>Generated by <strong>RepliMap</strong> Security Audit</p>
            <p class="mt-2 text-gray-500">
                Powered by <a href="https://www.checkov.io/" target="_blank" class="text-indigo-400 hover:text-indigo-300">Checkov</a> security scanner
            </p>
            <p class="mt-4 text-gray-600">{{ generated_at }}</p>
        </div>
    </footer>

    <script>
        function copyToClipboard(button) {
            const pre = button.parentElement.querySelector('pre');
            const text = pre.textContent;
            navigator.clipboard.writeText(text).then(() => {
                const originalText = button.textContent;
                button.textContent = 'Copied!';
                button.classList.add('bg-green-500');
                setTimeout(() => {
                    button.textContent = originalText;
                    button.classList.remove('bg-green-500');
                }, 2000);
            });
        }

        // Expand all findings on print
        window.addEventListener('beforeprint', () => {
            document.querySelectorAll('details').forEach(d => d.open = true);
        });
    </script>
</body>
</html>
